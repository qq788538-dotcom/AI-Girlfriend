# 虚拟人声音校准报告

> 2026-07-25 更新：本页前半部分的 Qwen3-TTS 0.6B“最终方案”是早期阶段结论，已被后续
> 原视频纯女声重建与人工盲听覆盖。当前正常说话基线为
> `Higgs-TTS-3-4B-bf16`、参考
> `reference-female-only-complete-11s.wav`、seed `20260817`。
> OmniVoice 与 LongCat AudioDiT 的 MLX 对照结果见
> `抖音博主虚拟女友复盘与语音升级.md` 第七节；新引擎在人工盲听通过前不接入实时链路。

## 最终方案

- 推理框架：oMLX 0.5.3（Apple Silicon / MLX）
- TTS 模型：`Qwen3-TTS-12Hz-0.6B-Base-4bit`
- 声线条件：原视频中提取的 5.35 秒干净中文人声
- 参考文本：`要是你跟别人聊天，我会吃醋的哦，我都开始胡思乱想了。`
- 采样参数：
  - `temperature=0.86`
  - `top_p=0.92`
  - `repetition_penalty=1.18`
  - `speed=1.0`
  - 24 kHz / mono / PCM16 原生流式输出

采用 Base 模型的参考音频条件生成，而不是继续使用 Vivian 预置音色。Vivian 的固定音色和
0.6B CustomVoice 不接受有效的情绪指令，单纯调整提示词无法解决高音、明亮、分段式播报感。

## 僵硬原因

| 指标 | 原视频参考 | 旧 Vivian | 参考克隆候选 |
|---|---:|---:|---:|
| 中位音高 | 255.7 Hz | 314.7 Hz | 222.6 Hz |
| 音高跨度 | 26.0 半音 | 11.77 半音 | 16.5 半音 |
| 长停顿平均值 | 224 ms | 585.5 ms | 426.7 ms |
| 音色亮度（频谱质心） | 1267.5 Hz | 2787.6 Hz | 1382.9 Hz |
| 有效能量起伏 | 10.93 dB | 8.36 dB | 9.35 dB |

旧 Vivian 的问题不是单纯语速慢，而是音高过高、频谱过亮、情绪音高跨度不足，并且在
标点之间生成过多长停顿，因此听起来像逐句播报。参考克隆把声线降到更温暖的区域，并恢复
更明显的情绪起伏。

## 实时短对话验证

测试文本：`你回来啦？我刚刚还有一点想你，不过才不会轻易承认呢。`

- 首个本地 PCM 块：358.6 ms（直接 TTS）
- 完整 5.28 秒音频生成：1.60 秒
- 实时系数：0.303
- 语音占比：94.1%
- 自然停顿：1 次，277 ms
- ASR 回转写：逐字正确

经过聊天模型和本地 WebSocket 网关的完整语音链路：

- 聊天文本完成：约 791 ms
- 首个音频事件：约 944 ms
- 完整回答结束：约 3.58 秒

## 文件

- 原声参考：`runtime/voice-calibration/reference-clean-5s.wav`
- 旧 Vivian 基线：`runtime/voice-calibration/current-baseline-same-text.wav`
- 最终实时样本：`runtime/voice-calibration/corrected-realtime-sample.wav`
- 同台词参考克隆：`runtime/voice-calibration/candidate-clone-expressive.wav`
- 声学分析脚本：`scripts/analyze-voice.py`
- 本地 TTS 延迟测试：`scripts/benchmark-local-tts.py`
- 候选生成脚本：`scripts/generate-voice-candidates.py`

## 使用

当前配置已经写入 `.env`。本机 oMLX 服务启动后，用以下命令开启纯本地试听：

```sh
VH_AVATAR_RENDERER_WS= VH_AVATAR_BACKEND=mock ./scripts/run-dev.sh
```

浏览器打开 `http://127.0.0.1:8765/`。该模式由浏览器直接播放声音，不会将参考声纹或合成
音频上传到云端。若启用远程人物口型渲染，合成后的 PCM 音频会被发送给渲染节点，应先确认
该声纹素材具备授权。

## 仙宫云音画联调

在获得明确上传授权后，已经将参考克隆语音接入仙宫云 FlashHead Lite。首次测试发现人物
预处理会阻塞 Uvicorn 默认 20 秒 WebSocket 心跳，现已做两项修复：

- WebSocket 心跳超时从默认 20 秒提高到 180 秒。
- 按人物参考图片 SHA-256 缓存预处理结果；相同人物的新会话直接复用。

修复后的热启动端到端数据：

- 聊天文本完成：约 0.88–0.98 秒
- 克隆语音首包：约 1.03–1.13 秒
- 首个带音频 MP4 分片：约 2.05–2.19 秒
- 完整回答渲染：约 5.52–7.01 秒（随回答长度变化）
- FlashHead 显存：约 5.93 GB

实际输出文件 `runtime/voice-calibration/cloud-avatar-cloned-voice.mp4`：

- H.264 Baseline，512×512，25 fps
- AAC，24 kHz，单声道
- 时长 12.683 秒
- 音视频轨时长偏差 0.067 ms
- 28/28 媒体分片成功返回

云端人物缓存的首次冷启动约 74 秒；只在 worker 重启或更换人物参考图后发生。人物缓存完成后，
日常对话使用上述约 2 秒首个视频分片的热启动路径。
