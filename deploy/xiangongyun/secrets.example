# Copy to secrets.env and chmod 600. Never commit the populated file.
#
# The all-local Xiangongyun runtime requires no cloud inference API key.
# This optional loopback token is sent only to same-host OpenAI-compatible
# services; current local servers do not require it.
VH_OMLX_API_KEY=
