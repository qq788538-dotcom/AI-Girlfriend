# 抖音博主虚拟女友复盘与语音升级

记录时间：2026-07-25
目标账号：天工开帧（抖音号 36776214178）

## 一、MuMu 实测结果

通过 MuMu 模拟器内的抖音搜索并进入账号主页，确认账号资料：

- 昵称：天工开帧
- 抖音号：36776214178
- 简介核心：独立 ComfyUI 动画师、AI 美少女内容
- 搜索 `VibeCoding` 后，账号内出现 5 条相关作品

其中最关键的公开作品有：

1. `赛博女友 Vibe Coding`
   - 封面明确写出首版技术栈：
     `Silero VAD + Whisper + llama.cpp + Qwen3-TTS`
   - 封面注明约 15GB 显存、本地运行、离线可用。
2. `我的赛博女友不可能这么可爱！满血版赛博女友 v1.1`
   - 观察时约 1.6 万赞，是当前最有代表性的成品展示。
   - 画面是暗光桌面/卧室环境中的年轻女性半身近景，托腮看向镜头。
3. `赛博女友彩蛋`
   - 账号说明技术栈见第一条视频，后续会继续发布部署内容。
   - 用户提供的 23.03 秒 MP4 正是这条作品中的演示片段。

## 二、画面为什么显得像真人

结论：真实感主要来自高质量人物源素材和克制的微运动，不是每一帧都由大型视频扩散模型实时生成。

从用户提供的视频逐帧观察：

- 人物始终保持托腮、半身、近镜头构图，肩膀和背景变化很小。
- 主要运动集中在嘴唇、眼睑、眼球、眉毛和很轻的头部摆动。
- 头发边缘、衣服和背景高度稳定，没有逐帧生成视频常见的纹理漂移。
- UI 中人物区域和左侧语音圆球明显是分层合成。
- 视频后段插入影视/梗图属于博主剪辑，不是实时虚拟人系统本身。

因此最接近它的实现不是“实时全身文生视频”，而是：

`高质量真人感人物图/短底片 + 音频驱动头像模型 + 前端 UI 合成`

现有项目采用 FlashHead 路线是合理的。若追求这条视频的稳定感，应优先：

- 固定半身姿势和机位；
- 限制头部运动幅度；
- 保留自然眨眼和轻微眼神移动；
- 让嘴唇运动跟随音素，但不要夸张张嘴；
- 避免实时改变发型、衣服、背景和大幅肢体动作。

人物是否“漂亮、像真人”，约 70% 由源人物图决定；驱动模型主要负责不要把这张图动坏。

## 三、声音升级实测

已下载并测试两个适配 Apple Silicon/oMLX 的新模型：

- `Qwen3-TTS-12Hz-1.7B-Base-bf16`
- `Qwen3-TTS-12Hz-1.7B-Base-8bit`

测试文本：

> 哥，你是不是有点不开心呀？别皱眉嘛，我刚刚只是逗你的。来，看着我……嗯，这样就好多了。

使用原视频中清理出的 5.347 秒女声作为零样本克隆参考。

| 模型/参数 | 首个 PCM | 生成耗时 | 音频长度 | RTF | 用途 |
|---|---:|---:|---:|---:|---|
| 1.7B BF16，温度 0.82 | 1903 ms（冷） | 7786 ms | 8.08 s | 0.964 | 离线高质量 |
| 1.7B 8-bit，温度 0.82 | 531 ms（冷） | 2529 ms | 8.48 s | 0.298 | 实时候选 |
| 1.7B 8-bit，平衡参数（热） | 191 ms | 2401 ms | 8.08 s | 0.297 | 推荐实时配置 |

推荐实时参数：

```text
temperature=0.90
top_p=0.95
repetition_penalty=1.10
```

推荐理由：

- 8-bit 在这台 M5 Max 上达到约 3.4 倍实时速度，留出了 ASR、LLM 和画面驱动的余量。
- 热启动首包约 190 ms，适合对话。
- 1.7B 相比原 0.6B 有更好的音色保持和句间情绪连续性。
- 已取消代码中不可配置的固定采样参数，并改为环境变量控制。

BF16 的音色与参考声线更接近，但实时系数接近 1，不建议作为默认实时模型。它适合离线成片、宣传样片或最终高质量导出。

## 四、当前落地配置

本机实时链路：

```text
Silero/WebRTC VAD
  -> Qwen3-ASR 0.6B 4-bit
  -> Qwen3.5 9B MLX 4-bit
  -> Qwen3-TTS 1.7B Base 8-bit
  -> 24 kHz PCM
  -> FlashHead 音频驱动
  -> 浏览器前端音画同步
```

质量模式：

```text
同一链路
  -> 将 TTS 切换为 Qwen3-TTS 1.7B Base BF16
  -> 离线生成完整音频
  -> 一次性渲染并复用人物参考特征
```

云端已关闭。语音、ASR、对话模型均可在 M5 Max 128GB 本地运行；只有在需要 FlashHead NVIDIA 推理时再临时开启云端 GPU。

## 五、下一轮验收标准

1. 连续听 20 句，不出现固定播音节奏。
2. 反问句、安慰句和轻微吃醋句的句尾方向不同。
3. 单句不出现超过 700 ms 的无意义停顿。
4. 热启动后 TTS 首包低于 500 ms。
5. 嘴型起始误差控制在 80 ms 内，结束误差控制在 120 ms 内。
6. 人物不说话时仍有自然眨眼和极轻微呼吸，但嘴部保持稳定。
7. 画面中头发、手指、衣服纹理不随说话漂移。

## 六、已生成的 A/B 样本

- `runtime/voice-calibration/qwen3-tts-1.7b-bf16-v11-style.wav`
- `runtime/voice-calibration/qwen3-tts-1.7b-8bit-v11-style.wav`
- `runtime/voice-calibration/qwen3-tts-1.7b-8bit-natural-v2.wav`
- `runtime/voice-calibration/qwen3-tts-1.7b-8bit-balanced-v3.wav`

`balanced-v3` 和 `v11-style` 两个 8-bit 样本在人工试听中被确认存在杂音、
发音边界模糊的问题，已废弃，不再作为实时默认。

当前默认改为：

- `Qwen3-TTS-12Hz-1.7B-Base-bf16`
- 去噪参考音频 `reference-clean-denoised-v2.wav`
- `temperature=0.75`
- `top_p=0.90`
- `repetition_penalty=1.05`

新样本为 `runtime/voice-calibration/qwen3-tts-1.7b-bf16-clean-v4.wav`。
本机实测首个 PCM 约 703 ms、RTF 0.823，ASR 核对内容完整。

## 七、博主补充截图：三模型与副语言标签

用户补充的博主截图列出：

- `OmniVoice`
- `LongCat AudioDiT`
- `Qwen3-TTS`

其中 `[laughter]`、`[sigh]`、`[question-en]`、`[surprise-ah]`、
`[dissatisfaction-hnn]`、`[confirmation-en]` 等写法是 OmniVoice 的原生
non-verbal token，不是 Higgs 或 Qwen3-TTS 的通用控制语法。不能把这些标签无差别发送给
任意 TTS。

截图中的“三幻神，看显存自行选择”应理解为三套候选底座；截图本身不能证明博主让三套模型
同时运行或逐句路由。项目采用先实测、再由人工盲听决定的方式。

### M5 Max 128GB 本地实测

全部使用 Apple MLX 转换模型和同一条
`reference-female-only-complete-11s.wav` 纯女声参考：

| 引擎 | 本地模型占用 | 峰值统一内存 | 热身后实测 | 当前角色 |
|---|---:|---:|---:|---|
| OmniVoice BF16（0.6B） | 1.5 GB | 约 3.0 GB | 约 1 秒生成 5.7–5.9 秒 | 笑、叹气、疑问、惊讶等副语言候选 |
| LongCat AudioDiT 1B BF16 | 5.3 GB | 约 8.8 GB | 4.89 秒生成 8.87 秒，RTF 约 0.55 | 高保真零样本克隆候选 |
| Higgs TTS 3 4B BF16 | 已安装 | 约 9.7 GB | 2.59 秒生成 4.32 秒 | 当前通过人工筛选的正常说话基线 |
| Qwen3-TTS 1.7B MLX | 已安装 | 见第三节 | 低延迟流式 | 延迟优先的备选，不作为当前音质冠军 |

M5 Max 128GB 可以运行这些模型。为了减少声线漂移，正式链路不会在一句话中途切换模型。

### 当前建议路由

1. 普通连续对话使用 Higgs，固定 seed `20260817`。三模型匿名盲听中 B（Higgs）人工胜出，
   LongCat 和 OmniVoice 不替换主声线。
2. OmniVoice 当前只开放人工确认自然的 `[question-en]` 疑问起声。
3. `[laughter]` 人工判断不自然，正式路由禁用；叹气、惊讶、不满轻哼等未试听标签默认禁用，
   逐项通过后才能进入白名单。
4. Qwen3-TTS 保留为低延迟回退；此前出现杂音或 AI 感的候选不恢复为默认。
5. 对话模型输出结构化的 `text` 与 `paralinguistic_event`，由路由器把事件转换为
   OmniVoice 标签；不允许聊天文本自行输出裸标签。

### 新盲听材料

- 三模型同文、同参考、等响度盲听：
  `runtime/voice-calibration/higgs-omnivoice-longcat-neutral/A.wav`
  `runtime/voice-calibration/higgs-omnivoice-longcat-neutral/B.wav`
  `runtime/voice-calibration/higgs-omnivoice-longcat-neutral/C.wav`
- OmniVoice 正常、笑声、疑问对照：
  `runtime/voice-calibration/omnivoice-mlx-comparison/01-neutral.wav`
  `runtime/voice-calibration/omnivoice-mlx-comparison/02-laughter.wav`
  `runtime/voice-calibration/omnivoice-mlx-comparison/03-question.wav`

人工结论：

- 主声线：B，即 Higgs 4B BF16、seed `20260817`。
- OmniVoice 笑声：不自然，禁用。
- OmniVoice 疑问起声：自然，加入副语言白名单。

只有白名单事件可以进入实时虚拟人链路。
